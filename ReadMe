# Password Maker

A simple Python script to generate a random password and save it to a file.

## Usage

1. Make sure you have Python installed.
2. Run the script:

   ```
   python PasswordMaker.py
   ```

3. Enter the desired password length when prompted.
4. The generated password will be displayed and saved to `generated_password.txt` in the same folder.

## Example

```
Enter password length: 16
Generated password: 8#kL!2v@xQz1$wP&
Password saved to generated_password.txt
```